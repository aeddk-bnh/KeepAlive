{
  "name": "keepalive",
  "version": "1.0.0",
  "private": true,
  "description": "KeepAlive - Automated Browser Session Monitor",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @keepalive/api & npm run dev --workspace @keepalive/web",
    "build": "npm run build --workspaces --if-present",
    "start": "npm run start --workspace @keepalive/api & npm run start --workspace @keepalive/web",
    "import": "tsx apps/api/src/cli/import.ts"
  },
  "devDependencies": {
    "rimraf": "^5.0.0",
    "typescript": "^5.0.0"
  }
}
